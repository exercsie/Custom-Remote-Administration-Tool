#include <cstdint>
#include <cstddef>
#include <cstdlib>
#include <iostream>

int x = rand();
const int KEY = x;

void caesarEncrypt(char *buf, size_t length) {
    
}

void caesarDecrypt(char *buf, size_t length) {
    
}